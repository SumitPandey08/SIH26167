stage_name: "stage3_vqa"
stage_index: 3
description: "Remote sensing spatial reasoning, object presence, counting, and area comparison QA."
target_datasets:
  - name: "RSVQA"
    task: "vqa"
  - name: "FloodNet"
    task: "vqa"

hyperparameters:
  epochs: 3
  batch_size: 8
  learning_rate: 1.0e-4
  image_size: 384
  prompt_template: "remote_sensing_vqa"
